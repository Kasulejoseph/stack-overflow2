language: python

python:
    - "3.5"

# install dependency
install: 
  - pip install -r requirements.txt
  - python3 -m pytest 
  - pip install pytest-cov
  - pip install coveralls

script:
  - pytest --cov = app/
  - coverage report
  
after_success: 
  - coveralls
